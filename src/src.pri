# This is parsed by both Qt Creator and CMake, so do not add anything
# here or change any formatting without making sure that both of these
# can parse your changes.

SOURCES += \
    src/main.cpp

HEADERS += \
